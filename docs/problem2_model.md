# 问题二：不同保边势函数对图像恢复效果的影响

> 对应题目《信号重构问题的相关探索》问题二。文档在调参与主实验完成后回填数值。

---

## 1. 问题背景

第二阶段恢复模型的保边性质完全由势函数 $\varphi_\alpha$ 决定：$\varphi_\alpha$ 偶、
凸、关于 $|t|$ 严格递增，且参数 $\alpha$ 的作用是使 $\varphi_\alpha(t)$ 在 $t$ 远离 0
时近似 $|t|$。题目列出 5 种候选（文献 [3] eq.(3)–(7) 的原文形式）：

| 编号 | $\varphi_\alpha(t)$ | $\alpha$ 的参数语义 | 光滑性 | 一阶导 |
|---|---|---|---|---|
| (1) | $\sqrt{t^{2}+\alpha}$ | 曲率/转折尺度 $\sqrt{\alpha}$ | $C^\infty$，$\varphi'\in(-1,1)$ | $t/\sqrt{t^{2}+\alpha}$ |
| (2) | $|t|^{\alpha},\ 1<\alpha\le2$ | 指数（非线性度） | $C^1$（$\alpha<2$ 时仅 $C^1$），导数无界 | $\alpha\,\mathrm{sgn}(t)|t|^{\alpha-1}$ |
| (3) | $\log\cosh(\alpha t)$ | 指数速率 $\alpha$ | $C^\infty$，$\varphi'\in(-\alpha,\alpha)$ | $\alpha\tanh(\alpha t)$ |
| (4) | $\dfrac{|t|}{\alpha}-\log\Big(1+\dfrac{|t|}{\alpha}\Big)$ | 阈值尺度 $\alpha$ | $C^2$，$\varphi'\in(-1/\alpha,1/\alpha)$ | $\mathrm{sgn}(t)\big(\frac1\alpha-\frac{1}{\alpha+|t|}\big)$ |
| (5) | Huber: $\frac{t^{2}}{2\alpha}\ (|t|\le\alpha)$，$|t|-\frac{\alpha}{2}\ (|t|>\alpha)$ | 二次区半径 $\alpha$ | $C^1$，$\varphi'\in(-1,1)$ | $t/\alpha\ (|t|\le\alpha)$，$\mathrm{sgn}(t)$ |

**说明**：题目中第 (3) 式印刷为 $\varphi_\alpha=\log(\cosh(\alpha))$，应为
$\varphi_\alpha(t)=\log(\cosh(\alpha t))$（文献 [3] eq.(5)），本文按后者实现。

## 2. 模型的定性差异

在相同二阶框架下（式 (7)：$\min_u \sum_{\Omega}\rho_\mu(u-y)+\beta\sum_{\text{边}\cap\Omega}\varphi_\alpha(u-\tilde u)$），
5 种势函数的差异体现在：

1. **$t=0$ 附近的二阶导**（平滑强度）：$\varphi''(0)$ 分别为 $1/\sqrt{\alpha}$、0（$\alpha>2$
   附近才>0；$1<\alpha<2$ 时 $\varphi''(0)=0$ 且 $t\to0$ 时 $\varphi''\to\infty$ 对 $1<\alpha<2$…见下）、
   $\alpha^{2}$、$1/\alpha^{2}$、$1/\alpha$。
2. **$|t|$ 大时的增长方式**：所有 $\varphi_\alpha$ 对大的差异近似线性（保边），
   但 (2) $|t|^{\alpha}$ 随 $\alpha>1$ 超线性（惩罚大于 $|t|$，介于 $|t|$ 与 $t^2$ 之间），
   (1)(3)(5) 渐近线性，(4) 线性（$\varphi(t)\approx|t|/\alpha-\log(|t|/\alpha)$，
   比例 $1/\alpha$ 由 $\beta$ 吸收）。
3. **梯度的全局性**：(1)(5) 的 $\varphi'$ 有界于 $(-1,1)$，(4) 有界于 $\pm 1/\alpha$，
   (3) 有界于 $\pm\alpha$，(2) **无界**（$|t|^{\alpha-1}$ 增长）——这对投影梯度算法的
   **条件数/迭代数**有直接可观测的影响（§5）。

## 3. 公平比较：跨势函数的参数标定

不同 $\varphi_\alpha$ 的量纲与灵敏度不同（同一 $\beta$ 下 (2) 的惩罚
$|t|^{\alpha}$ 在 $|t|=255$ 时约为 $|t|$ 的 $255^{\alpha-1}$ 倍，
如 $\alpha=1.4$ 时约 $6\times$），因此不能共用一套 $(\alpha,\beta)$。
本文采用**各自独立调优**：对每种势函数，先固定 $\beta=40$ 扫描 $\alpha$
（按各自参数语义取 $\alpha$ 网格），再在最优 $\alpha^{*}$ 下扫描
$\beta\in\{5,20,40,80,160\}$，得到 $(α^{*},β^{*})$；主实验在各自最优参数下比较。
这同时给出了**参数敏感性**的度量：越"平"的 PSNR-α 曲线说明该势函数越鲁棒。

## 4. 评价指标

在问题一指标（迭代次数 IT、CPU 时间、PSNR、SNR、MAE、Ω-PSNR）基础上，为刻画
"保边"增加：**SSIM**（结构相似性，衡量结构/纹理保持）与**边缘 PSNR**
（Sobel 梯度模长 >80 且膨胀 2 像素区域内的 PSNR，衡量边缘保持）。

## 5. 实验结果

### 5.1 参数标定（Lena 512，种子 2026）

**调参协议**：固定 $\beta=40$ 扫 $\alpha$（每函数按其参数语义设计网格），再在最优
$\alpha^{*}$ 下扫 $\beta\in\{5,20,40,80,160\}$；得到每种势函数在 30%/50% 噪声下的
最优参数（详表见 `exp/problem2/results/tables/problem2_tuning.txt`）：

| 势函数 | 最优 $(\alpha^{*},\beta^{*})$ @30% | PSNR\* (dB) | 最优 $(\alpha^{*},\beta^{*})$ @50% | PSNR\* (dB) |
|---|---|---|---|---|
| $\sqrt{t^{2}+\alpha}$ | (300, 40) | **37.373** | (300, 160) | 34.562 |
| $|t|^{\alpha}$ | (1.4, 5) | 37.366 | (1.6, 80) | **34.580** |
| $\log\cosh(\alpha t)$ | (0.1, 160) | 37.129 | (0.1, 160) | 34.329 |
| $|t|/\alpha-\log(1+|t|/\alpha)$ | (3, 160) | 37.355 | (3, 160) | 34.442 |
| Huber | (30, 40) | 37.211 | (30, 160) | 34.483 |

**$\alpha$ 敏感性（30%，$\beta=40$ 时的 PSNR 曲线，见
`results/figures/psnr_vs_alpha.png`）**：

- $\sqrt{t^2+\alpha}$：$\alpha\in[10,3000]$ 上 PSNR 为 36.99→37.37（峰）→37.25，
  平台宽阔、最鲁棒；$\alpha$ 越大大幅减少迭代数（$\alpha=3000$ 时仅 27 步）；
- $|t|^{\alpha}$：$\alpha\in[1.05,1.6]$ 均无法在 1500 步内收敛（$\varphi'$ 无界，
  条件数差），PSNR 仍缓慢爬升至 37.3；$\alpha=2$（纯二次）33 步收敛但
  PSNR 降至 37.14（$\ell_2$ 过度光滑）；
- $\log\cosh(\alpha t)$：**极敏感**——$\alpha=0.003$ 时 $\varphi'\le0.003$
  正则几乎失效，解退化为拟合污染观测，PSNR≈10.68（≈噪声图）；
  $\alpha\in[0.03,1]$ 内 32.7→36.8→36.1，峰在 $\alpha=0.1$；
- $|t|/\alpha-\log(1+\cdot)$：同病——$\alpha\ge30$ 时 PSNR 从 37.0 跌至 11.2
  （$\varphi'\le1/\alpha\to0$，正则失效）；
- Huber：$\alpha$ 取 1–300 均正常（$\varphi'\le1$ 恒有界），最鲁棒之一。

### 5.2 主实验（3 图 × 2 噪声等级 × 2 种子，各势函数用按噪声等级标定的最优参数）

汇总见 `results/tables/problem2_summary.csv`，均值如下：

**(a) r = 30%**

| 势函数 | PSNR | SNR | SSIM | 边缘PSNR | MAE | 迭代数 | 时间(s) |
|---|---|---|---|---|---|---|---|
| $\sqrt{t^{2}+\alpha}$ | **37.36** | 31.62 | 0.956 | **33.63** | 1.30 | **79** | **3.0** |
| $|t|^{\alpha}$ | 37.32 | 31.58 | 0.955 | 33.61 | 1.29 | 1500† | 119.2 |
| $\log\cosh(\alpha t)$ | 37.14 | 31.40 | 0.956 | 33.38 | 1.30 | 320 | 24.2 |
| $|t|/\alpha-\log(1+\cdot)$ | 37.34 | 31.60 | 0.955 | **33.63** | 1.28 | 466 | 28.4 |
| Huber | 37.18 | 31.44 | 0.956 | 33.40 | 1.33 | 99 | 3.8 |

**(b) r = 50%**

| 势函数 | PSNR | SNR | SSIM | 边缘PSNR | MAE | 迭代数 | 时间(s) |
|---|---|---|---|---|---|---|---|
| $\sqrt{t^{2}+\alpha}$ | **34.56** | 28.82 | 0.937 | **30.52** | 2.02 | **92** | **5.0** |
| $|t|^{\alpha}$ | 34.54 | 28.80 | 0.937 | 30.49 | 2.02 | 1500† | 170.5 |
| $\log\cosh(\alpha t)$ | 34.36 | 28.63 | 0.937 | 30.30 | 2.02 | 413 | 45.6 |
| $|t|/\alpha-\log(1+\cdot)$ | 34.49 | 28.76 | 0.936 | 30.46 | 1.98 | 530 | 48.8 |
| Huber | 34.41 | 28.67 | 0.936 | 30.33 | 2.06 | 287 | 19.9 |

† $|t|^{\alpha}\ (1<\alpha<2)$ 迭代触顶（1500 步未到终止容差），PSNR 已接近平台。

## 6. 结论

1. **恢复质量对势函数形式不敏感**：在各自标定的最优参数下，5 种势函数的
   PSNR 差距 $\le0.25$ dB（30%），SSIM 几乎相同（0.955–0.956），边缘 PSNR
   差距 $\le0.3$ dB——只要 $\alpha$ 使 $\varphi_\alpha$ 在典型的邻域差值范围内
   近似 $|t|$（$\ell_1$ 型线性惩罚），"保边"效果就是充分的；
2. **参数鲁棒性差异显著**：$\sqrt{t^2+\alpha}$ 与 Huber 对 $\alpha$ 有宽阔平台
   （推荐）；$\log\cosh$ 与 $|t|/\alpha-\log$ 在 $\alpha$ 偏大时因 $\varphi'$
   有界且趋于 0 而使正则"失效"，解退化为直接拟合污染观测（PSNR 从 37 跌至
   ~11）；$|t|^{\alpha}$ 的灵敏度居中但收敛性差；
3. **数值性能差异显著**：$\varphi'$ 有界且尺度均匀的 $\sqrt{\cdot}$、Huber
   数十步收敛；$\log\cosh$/$\log1$ 数百步；$|t|^{\alpha}\ (1<\alpha<2)$ 因
   $\varphi'$ 无界、曲率分布畸变（$|t|\to0$ 时 $\varphi''\to\infty$），
   上千步仍不收敛——**势函数的选择本身就是条件数的选择**，对大规模求解
   的可行性有直接决定性；
4. **综合建议**：实际应用中优先 $\sqrt{t^2+\alpha}$（质量优、鲁棒、快）或
   Huber；若坚持文献 [2] 的 $|t|^{\alpha}$ 经典选择，应配合更大迭代上限
   或采用 $\varphi'$ 有界的截断/光滑变体；
5. 由问题一的实验方法学可直接推广：本实验中"调参—鲁棒性—收敛性"三位一体的
   分析框架同样适用于其他势函数族。
